# Бэкап (04:00 UTC)
0 4 * * * /opt/silent-bot/scripts/daily_backup.sh

# Очистка (03:00 UTC)
# Выполняется автоматически через W10

# Мониторинг workers
* * * * * docker exec n8n_worker_health sh -c "redis-cli LLEN bull:n8n:waiting"